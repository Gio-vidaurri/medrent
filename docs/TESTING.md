# Tests

## Estado actual

- **No hay** suite automatizada (Vitest, Playwright, Cypress) en el repositorio.
- **Smoke test manual recomendado** antes de merge: `npm run build` y `npm run preview` en rutas críticas.

## Próximos pasos (fuera de este PR)

- Tests unitarios para `src/lib/**` (p. ej. Vitest).
- E2E críticos (home, blog, PLP, checkout de formulario) si el equipo lo prioriza.
